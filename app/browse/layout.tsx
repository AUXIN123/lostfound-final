// Page SEO for Browse
export const metadata = {
  title: "Browse Lost Items in Dhaka | Lost & Found",
  description:
    "Explore and search for lost items in Dhaka. Filter by category and contact the owner directly.",
};

export default function BrowseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
